apiVersion: v1
kind: Service
metadata:
  name: crypto-web
  namespace: crypto
spec:
  selector:
    app: crypto
    tier: web
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
  type: ClusterIP
