apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cryptoledger-postgres-pvc
  namespace: cryptoledger
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
